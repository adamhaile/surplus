{
  "name": "surplus-repo",
  "version": "0.4.0",
  "description": "Javascript pre-processor that converts JSX syntax into inline DOM instructions",
  "scripts": {
    "test": "karma start",
    "build": "tsc -p src/runtime/tsconfig.json && tsc -p src/preprocessor/tsconfig.json && rollup -c src/runtime/rollup.config.js && rollup -c src/preprocessor/rollup.config.js"
  },
  "author": "Adam Haile",
  "license": "MIT",
  "dependencies": {
    "s-js": "^0.4.3"
  },
  "devDependencies": {
    "jasmine": "^2.6.0",
    "karma": "^1.7.0",
    "karma-jasmine": "^1.1.0",
    "karma-chrome-launcher": "^2.1.1",
    "rollup": "^0.41.6",
    "typescript": "^2.3.2"
  }
}
